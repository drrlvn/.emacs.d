# -*- mode: snippet -*-
# name: impl Display for TypeName
# key: display
# --
impl Display for ${1:TypeName} {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "$0")
    }
}
