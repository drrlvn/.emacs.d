# -*- mode: snippet -*-
# name: impl FromStr for TypeName
# key: fromstr
# --
impl FromStr for ${1:From} {
    type Err = ${2:Error};

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Ok(Self{})
    }
}
